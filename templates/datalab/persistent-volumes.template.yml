---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ datalabVolume }}-claim
  namespace: {{ namespace }}
  annotations:
    volume.beta.kubernetes.io/storage-class: glusterfs-storage
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ dataVolumeSize }}
